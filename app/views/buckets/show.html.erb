

<div class="row">
		 <div class="col s12 m6">
			 <div class="card">
				 <div class="card-content">
					 <span class="card-title"><h1>Ready to Fill the Bucket?</h1></span>
           <h3> <%= link_to "Enter #{@bucket.name}'s bucket", bucket_lists_path(@bucket) %> </h3>

        <%= link_to 'All Buckets', buckets_path %> |
        <%= link_to 'Edit Bucket', edit_bucket_path(@bucket) %> |
        <%= link_to 'Delete Bucket', bucket_path(@bucket), method: :delete %>
				 </div>
			 </div>
		 </div>
<div>
